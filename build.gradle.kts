// ROOT build.gradle.kts
plugins {
    // We define the versions HERE so the app folder doesn't have to
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false
}
